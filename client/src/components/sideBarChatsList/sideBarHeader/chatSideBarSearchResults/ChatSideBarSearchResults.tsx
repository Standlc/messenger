import React, { useContext } from "react";

const ChatSideBarSearchResults = ({ searchInput }: { searchInput: string }) => {
  // const [filteredChats, ]

  return <div></div>;
};

export default ChatSideBarSearchResults;
